name: Petclinic CI with JFrog

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: petclinic
      IMAGE_TAG: 1.1.0
      BUILD_NAME: petclinic-build
      BUILD_NUMBER: ${{ github.run_number }}
      JFROG_URL: trialny4vkg.jfrog.io
      DOCKER_REPO: petclinic-docker-local

    steps:
    - name: Checkout source
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup JFrog CLI
      uses: jfrog/setup-jfrog-cli@v4
      env:
        JF_URL: https://trialny4vkg.jfrog.io
        JF_ACCESS_TOKEN: ${{ secrets.JFROG_TOKEN }}

    - name: Configure JFrog CLI Server
      run: |
        jfrog config add setup-jfrog-cli-server \
          --url=$JFROG_URL \
          --access-token=${{ secrets.JFROG_TOKEN }} \
          --interactive=false \
          --overwrite=true

    - name: Configure Maven via JFrog
      run: |
        mkdir -p jfrog-maven
        # Generate Maven settings.xml with Artifactory repos
        jfrog mvn-config \
          --server-id-resolve=setup-jfrog-cli-server \
          --server-id-deploy=setup-jfrog-cli-server \
          --repo-resolve-releases=maven-virtual \
          --repo-resolve-snapshots=maven-virtual \
          --repo-deploy-releases=petclinic-maven-local \
          --repo-deploy-snapshots=petclinic-maven-local
        # Copy generated settings.xml to custom folder
        cp ~/.m2/settings.xml jfrog-maven/settings.xml

    - name: Verify Maven Config
      run: |
        echo "===== settings.xml ====="
        cat jfrog-maven/settings.xml

    - name: Maven Build with JFrog
      working-directory: spring-petclinic
      run: |
        jfrog mvn clean package -DskipTests \
          --settings ../jfrog-maven/settings.xml \
          --build-name=${BUILD_NAME} \
          --build-number=${BUILD_NUMBER}

    - name: Docker Login to Artifactory
      uses: docker/login-action@v2
      with:
        registry: ${{ env.JFROG_URL }}
        username: ${{ secrets.JFROG_USER }}
        password: ${{ secrets.JFROG_TOKEN }}

    - name: Docker Build
      working-directory: spring-petclinic
      run: |
        jfrog docker build \
          --build-name=${BUILD_NAME} \
          --build-number=${BUILD_NUMBER} \
          -t ${JFROG_URL}/${DOCKER_REPO}/${IMAGE_NAME}:${IMAGE_TAG} .

    - name: Docker Push
      run: docker push ${JFROG_URL}/${DOCKER_REPO}/${IMAGE_NAME}:${IMAGE_TAG}

    - name: Publish Build Info
      run: |
        jfrog rt build-publish ${BUILD_NAME} ${BUILD_NUMBER}

    - name: Xray Scan
      run: |
        jfrog build-scan ${BUILD_NAME} ${BUILD_NUMBER}